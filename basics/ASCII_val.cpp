#include <iostream>
using namespace std;

int main() {
    char ch;
    cin >> ch;               // Take integer input
    int y = (int)ch;     // Explicitly cast ch to integer(ASCII value)
    cout << y;              // Output the float value

    return 0;
}
